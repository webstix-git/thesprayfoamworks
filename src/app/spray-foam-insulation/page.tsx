import { createMarketingPage, createPageMetadata } from '@/lib/create-page';

export const metadata = createPageMetadata('spray-foam-insulation');

export default function SprayFoamInsulationPage() {
  return createMarketingPage('spray-foam-insulation');
}
